[includeIf "gitdir:~/git/charlieroth/"]
  path = ~/.gitconfig-charlieroth
[includeIf "gitdir:~/git/waltyr-dacmus/"]
  path = ~/.gitconfig-waltyr-dacmus
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name = waltyr-dacmus
	email = waltyr-dacmus@tacittech.org
